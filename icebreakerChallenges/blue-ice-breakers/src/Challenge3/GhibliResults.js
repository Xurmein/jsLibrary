import React from 'react';

const GhibliResults = (props) => {
    props.results.map(title)
    return(
        <div>
            <h1>{this.title}</h1>
        </div>
    )}

    export default GhibliResults;