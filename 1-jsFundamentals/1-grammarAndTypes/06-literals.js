//LITERALS
//ex. "let x = 6"; x is a literal because the coder entered it
let days = ['mon', 'tue', 'wed', 'thu', 'fri', 'sat', 'sun'];
console.log(days);

console.log(days.toString());

let tired = true;

let soup = {
    a: 'corn chowda',
    b: 'split pea',
    c: 'borschte',
}

console.log(soup.c); //calls ".c" (or whatever) key from object cpntaining it

let car = 'Honda'

let dec = 10.2;

//hand-coded variables = literals