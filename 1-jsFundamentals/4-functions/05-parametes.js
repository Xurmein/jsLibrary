//PARAMETERS, like empty storage boxes we can name as chosen

function pet(animal){ //animal is the param
    console.log(`My pet is a ${animal}.`)
}

pet("tortugas");


function fullName(gName, sName){
    console.log("Hello, my name is " + gName + ' ' + sName);
}

fullName("Jake", "Peacock");